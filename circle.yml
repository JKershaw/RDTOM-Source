machine:
  php:
    version: 5.5.7
  environment:
    ENVIRONMENT: development

dependencies:
  post:
    - cp ~/RDTOM-Source/config/localhost /etc/apache2/sites-available
    - a2ensite localhost
    - sudo service apache2 restart

test:
  post:
    - sudo service apache2 restart	
    - php codecept.phar run acceptance --steps
    - ./show_output

deployment:
  master:
    branch: master
    commands:
      - ./deploy.sh