machine:
  php:
    version: 5.5.7
  environment:
    ENVIRONMENT: development

test:
  post:
    - php codecept.phar run unit --steps
    - php codecept.phar run functional --steps
    - ./show_output

deployment:
  master:
    branch: master
    commands:
      - ./deploy.sh