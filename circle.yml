machine:
  php:
    version: 5.5.7
  environment:
    ENVIRONMENT: development

dependencies:
  pre:
    - cp ~/RDTOM-Source/config/dev.rollerderbytestomatic.com /etc/apache2/sites-available
    - a2ensite dev.rollerderbytestomatic.com
    - sudo service apache2 restart

test:
  post:
    - php codecept.phar run acceptance --steps
    - ./show_output

deployment:
  master:
    branch: master
    commands:
      - ./deploy.sh