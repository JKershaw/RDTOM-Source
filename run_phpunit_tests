set -e

echo "~~~~~~~~~~~~~~~~~~~~~~~~~"
echo "Running PHP Unit tests"
echo "~~~~~~~~~~~~~~~~~~~~~~~~~"


for D in tests/unit/*; do
    if [ -f "${D}" ]; then
			DIR="$D.php"
			echo "testing $DIR"
			phpunit $DIR
	fi
done

echo "PHP Unit tests completed"
